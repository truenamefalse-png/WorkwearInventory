<Window x:Class="WorkwearInventory.IssueDocumentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Наряд на выдачу" Height="350" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="White">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="НАРЯД НА ВЫДАЧУ СПЕЦОДЕЖДЫ" FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock x:Name="EmployeeText" FontWeight="SemiBold"/>
            <TextBlock x:Name="DateText"/>
        </StackPanel>

        <ListBox Grid.Row="2" x:Name="ItemsList" BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ProductName}" Width="200"/>
                        <TextBlock Text="{Binding Quantity}" Width="50"/>
                        <TextBlock Text="{Binding UnitPrice, StringFormat={}{0:N2}}" Width="80"/>
                        <TextBlock Text="{Binding LineTotal, StringFormat={}{0:N2}}" FontWeight="Bold"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Общая стоимость:" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBlock x:Name="TotalText" FontWeight="Bold" FontSize="16"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Печать" Style="{StaticResource ModernButton}" Click="Print_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Сохранить" Style="{StaticResource ModernButton}" Click="SaveToFile_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>