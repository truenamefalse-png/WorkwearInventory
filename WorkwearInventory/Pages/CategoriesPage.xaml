<Page x:Class="WorkwearInventory.Pages.CategoriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Категории" Background="{StaticResource LightBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="NewCategoryBox" Style="{StaticResource ModernTextBox}" Width="200"/>
                <Button Content="Добавить" Style="{StaticResource ModernButton}" Click="AddCategory_Click"/>
            </StackPanel>
            <!-- ВАЖНО: убран DisplayMemberPath -->
            <ListBox x:Name="CategoryList" Grid.Row="1">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="Изм." Style="{StaticResource ModernButton}" 
                                        Background="#F39C12" FontSize="11" Padding="5,2"
                                        Click="EditCategory_Click" Tag="{Binding Id}"/>
                                <Button Content="Удал." Style="{StaticResource ModernButton}" 
                                        Background="#E74C3C" FontSize="11" Padding="5,2"
                                        Click="DeleteCategory_Click" Tag="{Binding Id}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </ScrollViewer>
</Page>